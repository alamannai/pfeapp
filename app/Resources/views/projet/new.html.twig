{% extends 'base.html.twig' %}

{% block body %}


     <a href="{{ path('projet_index') }}" class="btn btn-default">Retourner à la liste</a> 
     <div></div>
<br>
<div class="table-responsive">
    <div class="row">
        <div class="col-sm-12">
          <div class="well">
            <h1>Nouveau Projet</h1>

                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Create" class="btn btn-primary" />
                {{ form_end(form) }}
          </div>
        </div>
        
      </div>
    
 </div>    
    
{% endblock %}
